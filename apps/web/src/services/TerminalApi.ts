/**
 * TerminalApi — DEPRECATED.
 *
 * Terminal communication now goes entirely through TerminalService WebSocket (SSH Gateway).
 * This file is kept only as a tombstone. All imports should use TerminalService directly.
 */

export {}
